# pickup-dropoff
A web application that allows user to submit addresses of 1 pickup point and 1 drop-off point.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.